# Railway Nixpacks configuration
# Installs ffmpeg system binary so the Blend/Mix feature works in production.
# Railway uses Nix package manager — use nixPkgs, not aptPkgs.

[phases.setup]
nixPkgs = ["ffmpeg", "lame"]
