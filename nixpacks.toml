# Railway Nixpacks configuration
# Installs ffmpeg via Nix and symlinks it to /usr/local/bin so it's findable at a known path.
[phases.setup]
nixPkgs = ["ffmpeg", "lame"]
cmds = ["ln -sf $(which ffmpeg) /usr/local/bin/ffmpeg || true"]

